# Local collection - the cloud build keeps its own copy on the gh-pages branch
data/
state/
days/
cloud-data/
site/

# The WhatsApp Web session - this IS the login, never publish it
whatsapp_profile/

# Runtime
*.log
.vhr*.lock
__pycache__/
*.pyc

# Excel lock files
~$*
publish/
